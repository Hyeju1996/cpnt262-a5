let month = prompt("please enter");

const date = ['Mon','Tue', 'Wed', 'Thu',];

const count = date.push('Fri');
console.log(count);
console.log(date);

date.push ('Fri','sat', 'sun');
console.log(date);

for (let i = date; i <= month; i++){
    if (i === date);{

    }
console.log(i);
}
